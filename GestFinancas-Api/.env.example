# ===============================================
# GestFinancas API - Environment Variables
# ===============================================
# IMPORTANTE: Este é um arquivo de exemplo.
# Copie para .env e preencha com seus valores reais.
# NUNCA commite o arquivo .env com credenciais reais!
# ===============================================

# ============== DATABASE ==============
# Connection string do banco de dados
# Exemplo MySQL: server=localhost;port=3306;database=GestFinancasDb;uid=root;password=suasenha;
# Exemplo PostgreSQL: Host=localhost;Database=GestFinancasDb;Username=postgres;Password=suasenha
DB_CONNECTION_STRING=server=localhost;port=3306;database=GestFinancasDb;uid=root;password=root;

# ============== JWT AUTHENTICATION ==============
# Chave secreta para geração de tokens JWT (MÍNIMO 64 caracteres)
# CRÍTICO: Gere uma chave única e segura para produção!
# Use: openssl rand -base64 64 | tr -d '\n'
JWT_SECRET_KEY=ALTERE_ESTA_CHAVE_PARA_UMA_STRING_DE_NO_MINIMO_64_CARACTERES_SUPER_SEGURA_E_ALEATORIA_12345678901234567890

# Issuer do token JWT (quem emite)
JWT_ISSUER=GestFinancasApi

# Audience do token JWT (para quem é destinado)
JWT_AUDIENCE=GestFinancasUsers

# ============== CORS - Allowed Origins ==============
# Origens permitidas para CORS (separadas por vírgula)
# Desenvolvimento: http://localhost:4200,http://localhost:3000
# Produção: https://seudominio.com,https://www.seudominio.com
ALLOWED_ORIGINS=http://localhost:4200,http://localhost:3000

# ============== EMAIL CONFIGURATION ==============
# Configurações do servidor SMTP para envio de emails
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=seu-email@gmail.com
SMTP_PASSWORD=sua-senha-de-app
SMTP_FROM_EMAIL=noreply@gestfinancas.com
SMTP_FROM_NAME=GestFinancas

# ============== ASPNETCORE ENVIRONMENT ==============
# Ambiente de execução: Development, Staging, Production
ASPNETCORE_ENVIRONMENT=Development

# ============== LOGGING ==============
# Nível de log: Trace, Debug, Information, Warning, Error, Critical
LOG_LEVEL=Information

# ============== OPTIONAL: EXTERNAL SERVICES ==============
# Configurações opcionais para serviços externos

# Google Gemini API (se usar IA)
# GEMINI_API_KEY=sua-chave-gemini

# OpenAI API (se usar)
# OPENAI_API_KEY=sua-chave-openai

# Sentry (monitoramento de erros)
# SENTRY_DSN=https://seu-dsn@sentry.io/projeto

# Redis (cache)
# REDIS_CONNECTION_STRING=localhost:6379

# ===============================================
# COMO USAR:
# ===============================================
# 1. Copie este arquivo: cp .env.example .env
# 2. Edite .env com suas credenciais reais
# 3. Em desenvolvimento local, use dotnet user-secrets
# 4. Em produção, configure as variáveis no seu provedor de cloud
#    - Azure: Application Settings
#    - AWS: Environment Variables no Elastic Beanstalk/ECS
#    - Docker: arquivo .env ou -e flags
# ===============================================

# ===============================================
# GERAÇÃO DE JWT SECRET KEY SEGURA:
# ===============================================
# Linux/Mac: openssl rand -base64 64 | tr -d '\n'
# PowerShell: [Convert]::ToBase64String((1..64 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
# Online: https://generate-secret.vercel.app/64
# ===============================================
